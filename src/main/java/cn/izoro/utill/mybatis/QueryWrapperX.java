package cn.izoro.utill.mybatis;

import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;

/**
 * 拓展 MyBatis Plus QueryWrapper 类，
 *
 * @param <T> 数据类型
 * @Author zoro
 */
public class QueryWrapperX<T> extends QueryWrapper<T> {
    // todo 后续扩展需要用的方法
}
